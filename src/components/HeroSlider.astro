
---
import HeroSliderComponent from './HeroSlider.tsx';
import type { Work } from '../types/microcms';

interface Props {
  works: Work[];
}

const { works } = Astro.props;
---

<HeroSliderComponent works={works} client:only="react" />
